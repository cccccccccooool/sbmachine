# GPT-SoVITS / vLLM 服务在各自容器内，依赖不在此列。

# ── 核心链（config / LLM 调用 / 进度）──
requests==2.32.5
pyyaml==6.0.2
tqdm==4.67.3
tenacity==9.1.2

# ── Phase2 视觉（YOLO / OCR / 帧处理）──
numpy>=1.24.0,<=2.2.6
opencv-python==4.13.0.92
rapidocr-onnxruntime==1.4.4
ultralytics==8.4.62
torch>=2.4.0
torchvision>=0.19.0

# ── demo 数据（ticks.parquet 读取，demo_query.py）──
pandas
pyarrow
